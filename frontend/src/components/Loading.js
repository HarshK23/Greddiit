import { Typography } from "@mui/material"
import { Box } from "@mui/system"

const LoadingScreen = () => {
  return (
    <div>
      <Box justifyContent='center'>
        <Typography variant="body1">Loading dependencies</Typography>
      </Box>
    </div>
  )
}

export default LoadingScreen